---
title: "R Notebook"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Ctrl+Shift+Enter*. 

```{r}
library(tidyverse)
test_value_from_r <- 3
```

```{python}
isbn = '9788177581805'
print (isbn)
print (r.test_value_from_r)
```
```{r}
library(reticulate)
print (py$isbn)
```
```{r}
library(reticulate)
py_install ("isbntools",pip=TRUE)
```
```{python}
import isbntools
```

```{r}
library(reticulate)
print (py$isbn)
import("isbntools")
```

```{r}
library(reticulate)
conda_create("isbntools-env")

conda_install(packages="r-reticulate")
conda_install(packages="isbntools", pip=TRUE)

use_condaenv("isbntools-env")

isbntools<-import("isbntools")
```